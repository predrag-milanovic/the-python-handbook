# Not

We have one more logical operator to cover: `not`.

The `not` operator reverses a boolean value. It returns `False` when the input is `True`, and `True` when the input is `False`.

```python
print(not True)
# Prints False

print(not False)
# Prints True
```

Use `not` when you want to flip a condition before testing it.
