# Numbers Review

## Integers
In Python, numbers without a decimal point are called `integers` — whole numbers like `3` or `-3`.

## Floats
A `float` is the numeric type that stores numbers with a fractional part (decimal values).

```python
my_int = 5
my_float = 5.5
```
