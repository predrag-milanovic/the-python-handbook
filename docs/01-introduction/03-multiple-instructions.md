# Multiple instructions

Code runs in order, starting at the top of the program and moving down one instruction at a time. For example:

```python
print("this prints first")
print("this prints second")
print("this prints last")
```

Each `print()` instruction appears on a new line in the console.
