# Loops

This chapter introduces loops in Python so you can repeat actions efficiently. You'll learn when to use `for` and `while`, how `range()` controls iteration, and how indentation defines loop bodies and stopping behavior.

## Lessons

* [01 - Loops](./01-loops.md) - Learn core loop concepts, including `for` loops, `while` loops, `range()`, and common loop control patterns.
