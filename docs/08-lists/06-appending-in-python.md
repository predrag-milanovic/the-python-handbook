# Appending in Python

It's common to start with an empty list and fill it with values over time. To add a new item to the end of a list, use the `.append()` method.

```python
cards = []
cards.append("nvidia")
cards.append("amd")

print(cards)
# ['nvidia', 'amd']
```
